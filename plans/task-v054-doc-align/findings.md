# Findings & Decisions

## Requirements
- 用户原话（2026-09-07）：「文档完全没有对齐到当前最新，需要进行对齐。以及其他的就是有遗漏的地方也需要对齐。」
- 拆解：① 技能文档 vs 实际产物（脚本/规则数/config 键/模板数）对齐 ② 遗漏项补全（frontmatter/脚本/config 键文档化）③ 仓库簿记对齐（INDEX/未入库 plans/垃圾文件/v053 收尾）④ 修后重部署 + 9 位复验

## 📚 必要知识储备对齐记录
| 知识源 | 定位 | 已消费 | 结论落点 |
|--------|------|--------|----------|
| 双子代理审计报告 | 本文件 §Research Findings | ☑ | 全文 |
| deploy-flow 记忆（9 位拓扑+SOP） | ~/.zcode/cli/memories/projects/task-planner-skill-fba311568bf6d7b3/memory/task-planner-repo-deploy-flow.md | ☑（经审计代理读取） | §Research Findings-2 §4/§5 |
| 用户宪法 §十一 worktree 条款 | 会话注入 | ☑ | task_plan.md 隔离决策 |
| v053 deferred-issues（5 条） | plans/task-v053-skillfix-deploy/deferred-issues.log | ☑（经审计代理读取） | §Research Findings-2 §3 |

## Research Findings

### Findings-1：技能文档一致性审计（子代理 01，checkpoint: subagent-state/01-doc-audit.md）

**A. 漂移项 18 条**（sev | 位置 | 现状 → 期望）：
- A1 高 | SKILL.md:297 | "Rules 1-26" → 应 "Rules 1-27"（frontmatter L9 与正文 Rule 27 均为 27）
- A2 高 | SKILL.md:569 | 引 `templates/template-guide.md` → 实际在 `references/template-guide.md`（悬空）
- A3 高 | SKILL.md frontmatter | references 10 条列表缺 template-guide.md、template-mapping.md（references/ 实有 10 文件）
- A4 高 | config.json | 文档 4 处引 `config.json#autonomous_resume`（SKILL.md L19/L131/L310、critical-rules.md L148）但 config 无此键 → 补键
- A5 中 | README.md:67 | "Rules 1-12" → "Rules 1-27"
- A6 中 | references/batch-quality-gate.md:129 | "Rules 1-18" → 27 条现状
- A7 中 | INSTALL.md:238-241 | variant 模板"应 = 13" → 实际 12
- A8 中 | INSTALL.md:231 | 源路径 `scripts/plan-writer.md` 悬空 → `companion/agents/plan-writer.md`
- A10 中 | references/template-guide.md:81,84,93,102-103,247-248,265-269 | `${TASK_PLANNER_ROOT:-$HOME/dev/task-planner}` 陈旧兜底 → 实际根 /mnt/data/dev/task-planner-skill/skills/task-planner
- A11/A12 中 | SKILL.md:97,104-106,120,123,125,195,246,331 等 | `bash scripts/xxx.sh` 相对路径与自身路径规范冲突 → 加一次注记（`scripts/…` 均指 `<skill-root>/scripts/…`）而非逐处改写
- A13 低 | INSTALL.md:131 | companion "v2.2.1" 孤立版本号
- A14 低 | docs/ARCHITECTURE.md:150 + template-guide.md:59,61 | "v2.3" 与 README "v2.0(2026-08-29)" 无对应 → 以 config.json 版本为真源对齐
- A15 低 | README.md:64 | "config.json 13 键" → 实际 17 top-level properties（补 autonomous_resume 后 18）
- A16 低 | references/critical-rules.md:97 | 引 "Rule 26.3 处置" 但 26.3 子条款未单独标号 → 轻触补标号或改表述
- A17/A18 低 | references/template-mapping.md:82,104 | 外部项目示例路径 → 加 example 标注即可（不改值）
- 垃圾 | skills/task-planner/install.log（27KB，2026-09-04）已被 git 跟踪，.gitignore 未生效（先入库后声明）→ git rm

**B. 遗漏项**：
- B1/B5：frontmatter 缺 template-guide.md + template-mapping.md（正文 L302/L559/L569 三处引用却未收录）
- B2：零文档化脚本——plan-doctor.sh、resolve-plan-dir.sh、set-active-plan.sh、zcode-sessionstart.sh、check-complete.ps1、init-session.ps1（README 脚本节未收录）
- B3：零文档化 config 键——stale_remind_cooldown_calls、prompt_note_interval、plan_dir_pattern
- B4：companion/agents 三件套（plan-writer/article-batch-publisher/article-field-fixer）在 README/frontmatter 无收录（INSTALL 有散提及）

**C. 悬空指针**：C1=INSTALL.md:231（=A8）；C2=docs/ARCHITECTURE.md:126 `scripts/scan-plans.sh`（example）；C3=lib/install-companion.sh:172（example，lib 脚本不在本轮 scope，核实现状即可）；C4=scripts/sync-companion.sh:131（example，脚本文件不在 scope）；C5=SKILL.md:569（=A2）

**D. 清单基线**：references/=10 .md；scripts/=25；templates/=8；templates/variant/=12；docs/=1；companion/agents/=3；lib/=6；tests/=1；顶层 13 项（含垃圾 install.log）

### Findings-2：簿记/部署对齐审计（子代理 02，checkpoint: subagent-state/02-bookkeeping-audit.md）

1. **INDEX 对账**：INDEX 20 行 vs 实际 22 目录——缺 v054（本任务）、v055（并行会话 task-v055-scheduler-enforce，Phase 1 in_progress，属主会话负责登记）；v051/v052 行在 INDEX 标 complete
2. **git 入库**：已入库 18 个 plans 目录；**task-v051-canonicalize、task-v052-scheduler-positioning 未入库**（INDEX 已登记 complete）；脏项 = .active_plan(M) + aligned_files.json + v051/v052/v054/v055 目录
3. **v053 收尾**：Phase 6 = complete，outcome = COMPLETE（verification.md:116），委派统计（67%）与质量门控（Q1-Q6 全过）均已填；唯 task_plan.md:67 Current Phase 字段仍写 "Phase 6（收尾簿记）" 未收尾 → 本任务 Phase 2 修正。deferred-issues 5 条：① skill-fix validator 误报 ② 三兄弟技能未审计 ③ template-mapping §八备忘（无需行动）④ task-active-plan 挂账（已清）⑤ origin/main 陈旧（等用户决策，不在本轮）
4. **9 位部署清单**：zcode×3（task-planner/todo-skill/task-drift-guard）+ claude×4（task-planner/todo-skill/plan-resume/task-drift-guard）+ agents×1（plan-resume）+ opencode×1（task-planner）；全部实体目录（非 symlink）
5. **逐位 diff**：9/9 全部 IDENTICAL（基线 19a40ca 有效；e17df76 仅簿记）→ 修复合并前无需部署
6. **分支**：origin = git@github.com:napoler/task-planner-skill.git；master == origin/master（ahead 0）；本地 main 落后 93 提交且含 8 个独有旧同步提交（deferred #5，用户决策项，不动）

### Findings-3：执行期新事实（主进程直录）
- **并行会话确认**：本会话启动时 plans/ 无 v055；会话中途出现 plans/task-v055-scheduler-enforce/ 六件套且 .active_plan 被指到 v055 → 用户另一会话活跃中。处置：不碰其 scope/指针，INDEX 不代登记（写入 task_plan.md 强制约束）
- 部署位含 install.log（9 位与 canonical 全等 → 部署位同样带垃圾文件），本轮重部署（rm+cp）自然清除
- **INDEX 生成机制**：INDEX.md 头注释明确"Auto-generated by sync-todos.sh --index，勿手改"——簿记对齐必须走脚本刷新而非手工编辑；实测脚本自动收录 v054/v055 行并修正汇总计数（in_progress=2/complete=20）
- **attest-plan.sh 用法**：无参运行（项目根 cwd）自动锁定活跃计划；传参需传 task_plan.md **文件路径**（传目录路径报 "no task_plan.md found"）；v053 改动后重锁成功（SHA 6d4531e5）

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| A11/A12 相对路径用"注记"而非逐处改写 | 30+ 处出现，逐处改写噪音大且易引入新错；一次注记达到对齐效果 |
| 版本号以 config.json 为唯一真源 | README(v2.0)/ARCHITECTURE(v2.3)/INSTALL(v2.2.1) 三处互斥，需单一事实源 |
| autonomous_resume 补进 config 而非删文档 | Rule 24/SKILL.md 功能描述完整且设计如此，键缺失属 config 漏配 |
| C3/C4（lib/sync-companion 内 example 引用）只核实现状不改脚本 | 脚本文件不在本轮 scope（行为零变更约束） |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| Explore 档子代理连续 2 次 Provider rejected | Rule 22.3 兜底①改派 general-purpose → 成功（已登记 task_plan Errors 表） |
| 审计期间出现并行会话 v055 + .active_plan 被改指 | §十一.4 处置：不触碰、不争抢、INDEX 不代登记；已写入计划强制约束 |

## Resources
- canonical 源：/mnt/data/dev/task-planner-skill/skills/task-planner/
- 部署 SOP 记忆：memory/task-planner-repo-deploy-flow.md（rm + cp -rL + diff -r 复验）
- origin：git@github.com:napoler/task-planner-skill.git（master 跟踪）

### Findings-4：Phase 3 执行期事实修正（主进程复核直录）
- **install.log 真相修正**：它**从未被 git 跟踪**（.gitignore 一直有效——这也是 git status 从未显示它的原因）；审计代理"ignore 未生效"表述有误。实际只是 canonical 工作区磁盘残留 + 9 个部署位各有一份 → 处置 = Phase 5 主仓 `rm` 磁盘文件 + 重部署自然清除部署位；worktree 天然不含该文件，无 git 操作
- **A16 关闭（未修自愈）**：critical-rules.md Rule 26 段子条款 26.1-26.5 编号连续且 26.3 明确定义惩罚映射表 → L97 引用本可解析，零改动正确
- **autonomous_resume 落地形态**：config.json 的键均在 `properties` 下以 schema 对象（type/default/description）声明，新键同风格（default: true，描述引用 Rule 24.5），JSON 解析验证通过——文档引用 `config.json#autonomous_resume` 语义一致
- **MIGRATION.md**：全文件无与 README 版本史冲突的具体版本断言 → 零改动（审计 A 类中未单列，3a 主动核查）

## Visual/Browser Findings
-（无多模态输入）

---
<!-- [plan-compass] 提醒 = 本文件陈旧 → 立即回填再继续(Rule 19.7) -->
