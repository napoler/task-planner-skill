# P2-S4 Checkpoint（check-dispatch 双条件豁免 + selftest FG-05）

status: done
time: 2026-09-17
worktree: /mnt/data/dev/task-planner-skill-worktrees/task-v078-guard-fp-fixes

## 改动 1: check-dispatch.sh（fine_grain_checks ② 打包检测前插入双条件豁免）
- 位置: L264-268 新增（旁注 L264；if/else 豁免块 L265-277，既有 L264-269 逻辑整体进 else 分支一字未改；L45-47 注释口径未动）
- 条件: prompt 同时含「任务书」AND 字面 `subagent-state/`（双条件 grep -qF）
- 命中: stderr 一行 `[dispatch-guard] SKIPPED 打包检测: prompt 引用落盘任务书(Rule 35.3 范式), 打包判定以任务书内容为准`，n=0 短路，不累计 hits；warn/enforce 两档均不阻断
- 未命中: 走既有 grep -oE 'S[0-9]+' 计数分支（字节级不变）

## 改动 2: selftest-dispatch.sh
- 新增 FG-05（L278-289 区域）: prompt=fgc_prompt 合规基线 + 落盘任务书行（含「任务书」+ $FGC_PLAN/subagent-state/ 路径）+ 示例 ID（S1 先, S2 后, S3 收尾）
- 断言: stderr 含 `SKIPPED 打包检测` 且不含 `S-unit ID`
- FG-03（无豁免词多 S-unit 检出）原样保留作反向回归
- 头注释 L209-210 同步为 FG-01..05

## 自验证据
1. `bash selftest-dispatch.sh | tail -1` → `Total: 23 PASS=23 FAIL=0`（FG-03 PASS + FG-05 PASS 同批输出确认）
2. enforce 档实跑（临时夹具 pd，prompt 含「任务书」+subagent-state/ 路径+S1/S2/S3）: exit 0，stderr=`[dispatch-guard] SKIPPED 打包检测: ...`
3. 对照（sed 去「任务书」二字，grep -F 确认 CONTROL_CLEAN）: exit 2，stderr=`[dispatch-guard] ⚠ 单 prompt 检出 3 个 S-unit ID...` + `[dispatch-block] 🚫 ...多 S-unit 打包(3 个 ID)`
4. `bash -n` 两文件 → SYNTAX_OK；`git diff --stat` = check-dispatch +7 / selftest-dispatch +16-1（S4 范围=2 文件；zcode-posttooluse.sh +3 为并行 S3 产物，非 S4 改动，已留工作区未触碰）

## 遗留/交接
- 无。warn 档豁免语义与 enforce 同通道（stderr 提示 + 不计 hits），已由 FG-05（warn）+ 实跑 2/3（enforce）双向覆盖。
