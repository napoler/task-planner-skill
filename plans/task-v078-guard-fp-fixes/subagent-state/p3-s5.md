# S5 检查点 — task-v078-guard-fp-fixes / selftest-execution-stability.sh 提醒豁免行为用例

status: done
completed_at: 2026-09-17

## 前置基线
- 改前全量执行 `bash selftest-execution-stability.sh`：`Total: 17  PASS=17  FAIL=0`（既有用例 T1-T12 全 PASS，继续）
- 锚点实核：zcode-posttooluse.sh L100 既有 outcome 检查（只查 task_plan.md）+ L103-105 新增 verification.md 兜底分支（`[ -f "${plan_dir}/verification.md" ] && grep -qiE 'outcome: *(COMPLETE|BLOCKED)' ... && exit 0`，容前导空格）；提醒文案 plan-sync 在 L161/L234、plan-compass 在 L191/197/219/225
- T11a/b 范式（L93-143）：mktemp 临时计划目录 + stdin JSON + `env -u ZCODE_SESSION_ID bash "$POSTTOOL"`，采用同款手法；用干净 sid "sess-sfx12345"（canon 剥 sess 前缀后=sfx12345），不复用 sessabc123 state 文件

## 改动（仅 1 文件，+60/-1）
/mnt/data/dev/task-planner-skill-worktrees/task-v078-guard-fp-fixes/skills/task-planner/scripts/selftest-execution-stability.sh
- 新增 T13（插在 T12 段之前，执行顺序 T11→T13→T12，命名跟随 T11 正/负例分离计入 Total 的体系）：
  - T13a 正例（st13_pos，L172-181 一带）：fixture `$T/plans/task-y`，task_plan.md 陈旧 mtime（touch -d "2 hours ago"，无 outcome 字样，满足 L100 不命中）+ verification.md 内容=` outcome: COMPLETE`（前导空格，模拟真实落盘格式）→ 执行 POSTTOOL 输出为空（豁免生效，无 plan-compass/plan-sync 提醒）
  - T13b 因果对照（st13_neg）：同 fixture 删 verification.md 再执行 → 输出含 plan-sync 陈旧提醒（age_min≈120 ≥ 15min 阈值触发）；诊断失败时输出实际 out 供定位。证明豁免由兜底分支因果生效，非环境巧合
  - 公共子程序 st13_fixture/st13_exec；每用例清理临时目录与 `/tmp/task-planner-hook-sfx12345.state`（T11 同款手法）
  - 头注释用例清单同步：`# 12 组用例 T1-T13 (T13 为 task-v078 S5 行为用例...)`
- 关键证据原文（工作树）：`Total: 19  PASS=19  FAIL=0`，其中 `[PASS] T13a S5 正例: 陈旧计划+verification.md(前导空格 outcome: COMPLETE) → POSTTOOL 输出无 plan-compass/plan-sync 提醒(兜底豁免生效)` 与 `[PASS] T13b S5 因果对照: 同 fixture 删 verification.md → 输出含 plan-compass/plan-sync 陈旧提醒(豁免由兜底分支因果生效, 非环境巧合)`

## 自验结果（3 条逐项）
1. 前置基线 Total=17/17/0；改后全量 `Total: 19  PASS=19  FAIL=0` — T13a/T13b 新增 2 断言 PASS，既有用例 T1-T12 无回归，FAIL=0（含 T11a/b 回归保护）
2. `bash -n` 通过（输出 SYNTAX-OK）
3. `git -C <worktree> diff --stat`：`.../scripts/selftest-execution-stability.sh | 61 +++++-`（1 file changed, 60 insertions(+), 1 deletion(-)），`git status --short` 仅该文件 ` M`

## findings
- 无。T13a 断言为「输出为空」而非 grep 不含 plan-compass/plan-sync：fixture 无 findings/progress 文件、单文件调用 count=1 < todo_n=10，唯一可命中的提醒就是被豁免的 plan-sync，空输出是「不含该类提醒」的充要证明；T13b 触发路径为 L161 plan-sync（findings/progress 分支因文件不存在不命中）。
