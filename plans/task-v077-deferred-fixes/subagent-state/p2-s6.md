# P2 S6 checkpoint

status: done
time: 2026-09-17
scope: S-unit ID=纯数字 契约在两处文件落位（plan-writer 契约表 + task_plan 模板 S-unit 注释块）

## 改动
1. skills/task-planner/companion/agents/plan-writer.md L117 新增契约表行 `| **s_unit_id** | ... 纯数字 ... |`（紧随 L116 knowledge_brief 行之后，3 个 `|` 与 L116 一致）
   - 注：任务书原文行内嵌正则片段 ``` `^\|\s*S[0-9]+\s*\|` ``` 含字面 `|`，直接写入会破坏 markdown 表结构（该行 pipe 数 5 ≠ 表标准 3）；已最小化改写为「数据行正则 S[0-9]+ 边界不认字母后缀」，语义等价，未引入新 `|` 字符。
2. skills/task-planner/templates/task_plan.md L187 在 ①②③ 之后追加注释行 ④：「④ ID 列一律纯数字（S1/S2…），禁字母后缀——check-plan-dispatch.sh 数据行正则不认 S2a 式命名，attest 拒锁（task-v076 教训）」

## 自验结果
- grep '纯数字'：plan-writer.md L117 ×1，task_plan.md L187 ×1（改动前均为 0）
- plan-writer.md L116/L117 pipe 数均 = 3（对齐既有表行列格式）
- git diff --stat 本次新增仅这 2 文件各 +1 行；此前单元已改 4 文件（README.md、batch-quality-gate.md、selftest-smart-merge.sh、smart-merge-back.sh）原样保留

## evidence
- plan-writer.md:117 `| **s_unit_id** | S-unit 表 ID 列一律纯数字...`
- task_plan.md:187 `④ ID 列一律纯数字（S1/S2…），禁字母后缀...`
