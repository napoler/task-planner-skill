# S3 检查点 — smart-merge-back.sh 假 IDENTICAL 根因修复（executor, 2026-09-17）

## 状态
S3 done。仅改 1 文件：`/mnt/data/dev/task-planner-skill-worktrees/task-v077-deferred-fixes/skills/task-planner/scripts/smart-merge-back.sh`（41+/10-，worktree git status 仅该文件 M）。

## 改动落点（最终行号）
| # | 改动 | 位置 |
|---|------|------|
| 1 | DEPLOY_SRC 推导 + 注释（部署源=主仓 canonical；SKILL_ROOT 仅保留守卫/自位提示） | L354-357 |
| 4 | fail-closed 校验上移至 DRIFT 初始化后（防 R3 P3 类「初始化置 0 吞标志」缺陷） | L367-371 |
| 5 | 循环内源缺失逐位跳过（validate_slot 之后 → REJECTED 诊断语义不变） | L510-518 |
| 2 | cp 源 → DEPLOY_SRC | L528 |
| 3 | diff 基准 → DEPLOY_SRC + 基准注释 | L554-558 |
| 5b | 自位 REJECTED 指引（slot==SKILL_ROOT 时追加「运行位自保护; 请改用主仓副本运行或手动 rm+cp 部署该位」） | L476-481 |
| 6 | 头注释 L15-17/L44/L84/L89/L348-349/L560 + L346-349 修复注记 | 同上 |
| 7 | IDENTICAL/OK echo 文案补「基准=主仓 skills/task-planner」 | L556/L566 |

## 自验记录（全部实跑）
1. `grep -c 'DEPLOY_SRC'` = **10**（≥5）
2. cp L528 / diff L555 均引用 $DEPLOY_SRC；守卫 L388 `GUARDS="...$SKILL_ROOT:full|..."` 保留
3. `bash -n` PASS
4. selftest（worktree 内 `bash skills/task-planner/scripts/selftest-smart-merge.sh`）：
   - Total: **14 PASS=13 FAIL=1**
   - FAIL 仅 **SM-06**：`rc=6(期望6) s1-IDENTICAL=缺 s2-DRIFT=在` — 根因=fixture 主仓无 `skills/task-planner` 目录（DEPLOY_SRC 缺失 → s1 跳过部署无 IDENTICAL 行）+ 自测侧 L219 锚未含新文案「(基准=主仓 skills/task-planner)」。fixture 扩展属下一单元 S4 范围（knowledge-brief 易错点 2），未改 selftest（符合本单元约束）。非语法/逻辑崩。
5. 手工冒烟（fake 主仓 + TASK_PLANNER_DEPLOY_SLOTS 注入）：
   - 源存在：`rc=0` + `[DEPLOY] IDENTICAL: /tmp/s3-ok/smoke5-slot (基准=主仓 skills/task-planner)` + slot 终态 MARKER=v077-canonical + `diff -rq`=0
   - 源缺失：`rc=6` + `[DEPLOY] DRIFT: (部署源不存在: ... 禁回退 SKILL_ROOT)` + slot 原样保留（keep-me）
   - 自位（脚本从部署位运行、slot==SKILL_ROOT）：`rc=6` + `REJECTED: ... (= 受保护路径 ...)(= 运行位自保护; 请改用主仓副本运行或手动 rm+cp 部署该位 — 部署源=主仓 skills/task-planner)`
   - 源缺失 + 危险三 slot：3 条 REJECTED 原样输出（validate_slot 诊断不受 skip 影响），rc=6

## 过程发现（供 S4 消费）
- selftest SM-06 双因 FAIL：①fixture 无主仓 skills/task-planner（S4 mk_fixture 补建 + 内容标记，即 knowledge-brief 易错点 2）②L219 grep 锚 `[DEPLOY] IDENTICAL: $T6/s1` 精确匹配，新文案追加了后缀 — S4 需同步自测侧断言口径
- 部署源缺失场景下 validate_slot 先行、skip 后置的设计使 REJECTED 用例（SM-08/09/10/12/13）在 fixture 未造主仓内容时仍 PASS（本轮实测 13/14）
- 未引入新 env 键；未动守卫/换位原子性/trap

## 恢复点
S4 直接改 selftest-smart-merge.sh（mk_fixture 加 `mkdir -p "$main/skills/task-planner" + 标记`，SM-06 L209-219 自测侧同步，新增 SM-14 陈旧副本用例）。
