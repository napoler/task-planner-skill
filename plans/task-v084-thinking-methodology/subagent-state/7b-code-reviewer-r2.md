# S7b Code Review Gate 轮 2 — checkpoint (status: done)

- 范围：worktree /home/terry/task-planner-skill-worktrees/task-v084-thinking-methodology，聚焦 fix 提交 8a4320d（`git show HEAD` = 2 文件 +12/-8：methodology.md 3 行改写 / selftest-methodology.sh 头注释 + M-12 块）；只读审查，零业务文件修改（本检查点 + findings 追加为委派指令要求）
- 手法：fix diff 逐行 + 关键行 Read + grep 复现原文 + 突变实测（复制 skills/ 到 /tmp/mut-test 后按节删行跑真脚本，五节全矩阵）

## 四项逐条核验
1. selftest:7 注释精确化 —— **PARTIAL**。`selftest-methodology.sh:7` 已改「R/Q 九条方法名关键词 (… ≥9; T1-T5 由 M-12 另计)」（精确化达标）；但 `:76` 仍为「# M-03: methodology.md 含 9 条方法名关键词」——轮 1 原文点名的是「:7（及 :76 注释）」，行内注释未同步。
2. T3 例外注记 —— **FIXED**。`:11` 改为「失败惩罚原则上引用 … ；**唯一显式例外=T3 金字塔原理（提醒级…见 §思维方法论 T3）**」，`:230` 同步「唯一例外 T3 金字塔原理=提醒级（见 §思维方法论 T3）」，与 `:126` T3「显式例外（不进 Rule 26.3 硬门）」字面一致；全文 grep「例外」= 3 行（11/126/230），无残留「全部失败惩罚引用」绝对措辞（原 :230 绝对句已软化）。
3. 突变复测 M-12 逐条 —— **NOT-FIXED（T3 仍可穿透）**。实测矩阵：删 T1 节→M-12 FAIL；删 T2 节→M-12+M-13 FAIL；删 **T4 节→M-12 FAIL**；删 T5 节→M-12 FAIL；删 **T3 节（119-128 行）→ 16/16 全 PASS**。根因：关键词「金字塔原理」除 T3 节外还存在于 `:11`（本次 fix 新增的例外注记）、`:92`（章导语引用户原话）、`:230`（关系表惩罚统一行），M-12 逐条循环命中的是这些残留文本。故 `:183` 注释「任一 T 条款被删即红，防聚合口径穿透」对 T3 为**不实声明**。更正：轮 1 建议的 `Minto≥1` 锚同样无效（T3 节删除后 Minto 仍=1，因 T4 出处 `:132`「MECE（Minto 金字塔姊妹法）」），有效锚须为标题锚 `^### T3 金字塔原理`=1（未改文件实测=1）或 T3 专属短语「结论先行、以上统下」。
4. T2 层数措辞 —— **FIXED（附一条记录同步项）**。`:111` 改为「连问为什么 ≥5 层（用户纪律「不少于五个为什么」）；每层须真实推进（禁凑数反问），触达不可再分的根因层/本质约束即收敛（收敛语义对齐 Rule 31.2；其 ≤5 层为错误侧单链上限，问题侧允许更长链）」——原「对齐上限语义」牵强句已删，且显式把 ≤5 归位为错误侧口径，与 R4 `:85`／critical-rules.md:257「≤5 层」不再字面冲突，无新矛盾。但与 findings.md `:20`「上限语义对齐 31.2」/`:24`「下限 5 上限对齐」的冻结措辞已不一致（落地改为「问题侧允许更长链」= 无上限），属 stale 记录，建议 P5 簿记时一行同步，非代码改动。

## fix diff 新问题扫描
- 纯增量性质保持：仅 2 文件、3 行 methodology 改写 + 1 行注释 + M-12 块；零 critical-rules 触碰、零既有条款语义删除、零新 config 键。
- M-12 新块 shell 正确性 PASS：`set -u` 无 `set -e` 下 `T_RED` 初始化/`${c:-0}` 兜底/`break` 后 `[ "$T_RED" -eq 0 ]` 取 rc 均正确；文件缺失时 `grep` 输出空 → 兜底 0 → 变红（fail-closed 合理）。
- 唯一新增缺陷即第 3 项（注释不实声明）；另有 2 条轻量精度项（`:76` 残留、`:11`「唯一」措辞对 `:146` T5／`:221` Q5 亦不引用 Rule 26.3 而言略过强）。
- 未覆盖既有断言回归：实跑 16/16 PASS，与轮 1 基线一致（M-03 实测 37、M-13 1/2、M-14 4/0、M-15 1/1、M-16 1）。

## 最终结论
REVIEW_VERDICT: CHANGES_REQUESTED
理由：无 Blocker（安全/数据/功能无损；16/16 绿；纯增量成立；第 2、4 项矛盾已实质消除）。但第 3 项为轮 1 四项修正之一，fix 提交自报「M-12 逐条锚定防穿透」而突变实测 T3 节整节删除仍 16/16 PASS，且 fix 自身写入的注释构成不实声明，故不可判 APPROVED。修复成本 = 1 行（M-12 循环增标题锚 `^### T3 金字塔原理` 或改 T3 锚为「结论先行、以上统下」）；建议顺带同步 `:76` 注释。
